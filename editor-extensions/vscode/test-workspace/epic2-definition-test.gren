module DefinitionTest exposing (main, greet)

import Node

main : Node.Program {} {}
main =
    Node.defineProgram
        { init = \_ -> ( {}, Node.none )
        , update = \_ model -> ( model, Node.none )
        , subscriptions = \_ -> Sub.none
        }

greet : String -> String
greet name =
    "Hello, " ++ name ++ "!"

useGreet : String
useGreet =
    greet "World"