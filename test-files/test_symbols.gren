module TestSymbols exposing (..)

type alias User = 
    { name : String
    , age : Int
    }

type Status = Active | Inactive

length : String -> Int
length str = String.length str

add : Int -> Int -> Int
add x y = x + y

main : Program () Model Msg
main = Browser.sandbox { init = init, update = update, view = view }